# MongoDB Connection String
# Get a free MongoDB Atlas cluster at: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/ai-journal

# Groq API Key for AI features
# Get your free API key at: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# JWT Secret for authentication tokens
# Generate a secure random string (you can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Port (optional, defaults to 8000)
PORT=8000

# Node Environment (optional)
NODE_ENV=development
